Bank
1. Много пропусков между строками, считается плохим стилем
2. Импорт классов с одно и того же пакета, проще сделать импорт класса целиком
3.

Sorted
1. Много пропусков между строками, считается плохим стилем. Точнее между пакетом и ипортом
2. Конструктор по умолчанию не нужен, он и так существует
3. Пробелы между именем метода и скобками не нужен
4. Название методов должны быть  1 слово с маленькой буквы, остальные с заглавной

Convert
1. Много пропусков между строками, считается плохим стилем. Точнее между пакетом и ипортом
2. Конструктор по умолчанию не нужен, он и так существует
3. Такие комметарии лучше не оставлять, желательно пользоваться JavaDoc
4. У условий и циклов отсутствуют фигурные скобки {}.
5. int cls лучше разделить, плохо читаемый код.

Account
1. Поля класса имеют модификатор доступа default, лучше скрыть и использовать гетеры и сеттеры
2. String otvet разделили объявление строки и сохраниния в нее данных.
3. Лишние пробелы в методах.
4.